# X404X Autonomous Red Team Platform — Requirements
# ====================================================
# Generated: 2026-06-11
# Fases 0-4 completas: ~12,700 líneas en 45 módulos

# Core Runtimes
go>=1.22.0
python>=3.11
node>=22.0

# Python — Bridge Core
cryptography>=41.0
numpy>=1.24
requests>=2.31
PyYAML>=6.0
scapy>=2.5.0
websocket-client>=1.6.0
rich>=13.0
click>=8.0

# Python — AI & Deep Learning
ollama>=0.1.0
torch>=2.0
onnxruntime>=1.16
opencv-python>=4.8
dlib>=19.24

# Python — Voice & TTS (Deepfake Vishing)
# TTS>=0.22.0
# pyVoIP>=0.2.2
# twilio>=9.0

# Python — Document Export (SPECTER)
fpdf2>=2.7.0
python-docx>=1.1.0

# Python — QR Generation (Hydra QR Worm)
qrcode>=7.4
Pillow>=10.0

# Python — Modbus/SCADA (Hydra Vectors)
pymodbus>=3.5.0

# Python — Android ADB (USB ADB)
# pure-python-adb>=0.3.0

# Python — SDR (RF Contagion)
# pyrtlsdr>=0.2.93
# hackrf>=0.0.1

# Python — Testing
pytest>=8.0
pytest-asyncio>=0.23.0

# Go Modules (managed via go.mod)
# google.golang.org/grpc>=1.64.0
# google.golang.org/protobuf>=1.34.2
# github.com/gorilla/websocket>=1.5.0
# golang.org/x/crypto>=0.28.0
# golang.org/x/sys>=0.28.0
# golang.org/x/net>=0.29.0
# golang.org/x/net/websocket>=0.29.0
# modernc.org/sqlite>=1.32.0

# Go Build Tools (for payload generation)
# garble — go install mvdan.cc/garble@latest
# upx-ucl — apt install upx-ucl

# Go TinyGo (for WASM/Bridge modules)
# tinygo — apt install tinygo / go install tinygo.org/x/tinygo@latest

# Docker Lab
docker>=27.0
docker-compose>=2.24

# npm Dashboard (Vue 3)
# vue>=3.4
# vite>=5.0
# pinia>=2.1
# tailwindcss>=3.4
# chart.js>=4.4
# apexcharts>=3.45
# d3>=7.9
# xterm>=5.3

# Linux Tools (Hydra + RF + SDR)
# nmap — apt install nmap
# ffmpeg — apt install ffmpeg
# rtlsdr — apt install rtl-sdr
# hackrf — apt install hackrf
# modemmanager — apt install modemmanager
# iproute2 — apt install iproute2
# homeplug — apt install homeplug-utils
# afl++ — apt install afl++
